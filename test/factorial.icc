#	
	*-------------------...-*
	| Factorial - iterative |
	| F(n) = n!             |
	*-----------------------*
#	
{
	stdin = "/dev/stdin"; 
	echo "F(n) - input 'n'"; 
	n = load stdin;
	¿n>=0? {
		fact = 1;
		∀ i ∈ ]2,n+1[: fact *= i.
	}.
}
